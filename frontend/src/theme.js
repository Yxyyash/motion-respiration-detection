import { extendTheme } from '@chakra-ui/react';

const theme = extendTheme({
  fonts: {
    body: 'YourPreferredFont, roboto',
    heading: 'YourPreferredFont, roboto',
  },
});

export default theme;